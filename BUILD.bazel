load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "typeof",
    visibility = ["//visibility:public"],
    hdrs = glob(["include/**/*.hpp"]),
    includes = ["include"],
    deps = [
        "@boost//preprocessor",
        "@boost//type_traits",
        "@boost//config",
    ],
)
